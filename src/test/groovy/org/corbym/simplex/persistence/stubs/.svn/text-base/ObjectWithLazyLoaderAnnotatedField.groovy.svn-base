package org.corbym.simplex.persistence.stubs

import org.corbym.simplex.persistence.annotations.Id
import org.corbym.simplex.persistence.annotations.LazyLoad

class ObjectWithLazyLoaderAnnotatedField {
    @Id
    def id
    @LazyLoad
    def somefield;
}
